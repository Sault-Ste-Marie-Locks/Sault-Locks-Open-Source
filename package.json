{
  "name": "lock-release-desktop",
  "version": "0.0.0",
  "description": "Lock Release real desktop app with startup updates enabled and forced update check",
  "main": "main.js",
  "private": true,
  "author": "Lock Release",
  "productName": "Lock Release",
  "scripts": {
    "start": "electron .",
    "build-win": "electron-packager . \"Lock Release\" --platform=win32 --arch=x64 --overwrite --out=dist"
  },
  "devDependencies": {
    "electron": "latest",
    "@electron/packager": "latest"
  }
}