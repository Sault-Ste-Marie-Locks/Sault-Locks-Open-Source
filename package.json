{
  "name": "lock-release-desktop",
  "version": "0.0.0",
  "description": "Lock Release desktop app with tray background controls, local date/time logging, and V23 GitHub JSON import repair",
  "main": "main.js",
  "private": true,
  "author": "Lock Release",
  "productName": "Lock Release",
  "scripts": {
    "start": "electron .",
    "build-win": "electron-packager . \"Lock Release\" --platform=win32 --arch=x64 --overwrite --out=dist"
  },
  "devDependencies": {
    "electron": "latest",
    "@electron/packager": "latest"
  }
}
